#include<stdio.h>

// void calculate(int n1, int n2, int n3);

int main() {
	int i, n = 0, sum = 0;
	
	for (i = 0; i < 3; i++) {
		printf("Hello, World!\n");
	}
	
	return 0;
}

/*
void calculate(int n1, int n2, int n3) {
	printf("Enter number 1: ");
	scanf("%d", &n1);
	printf("Enter number 2: ");
	scanf("%d", &n2);
	printf("Enter number 3: ");
	scanf("%d", &n3);
	
	if ((n1 < n2) && (n1 < n3)) {
		printf("%d is the smallest integer.", n1);
	} else if ((n2 < n1) && (n2 < n3)) {
		printf("%d is the smallest integer.", n2);
	} else {
		printf("%d is the smallest integer.", n3);
	}
}

*/


